
    <Window x:Class="RosteringApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RosteringApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <Grid>
        <ListView Name="EmployeeList" ItemsSource="{Binding Employees}" Padding="0,25,0,0">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction
                Command="{Binding OpenEmployeeShiftWindow}"
                CommandParameter="{Binding SelectedItem, ElementName=EmployeeList}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding FullName}" Header="Name"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Role}" Header="Role"/>
                </GridView>
            </ListView.View>
        </ListView>
        <DockPanel LastChildFill="False"/>
        <ToolBar Width="400" HorizontalAlignment="Center" VerticalAlignment="Top" Height="25">
            <Button Command="{Binding OpenEmployeeAddWindow}">Add Employee</Button>
        </ToolBar>
    </Grid>
</Window>
